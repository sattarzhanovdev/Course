export const scrollHome = () => {
  window.scroll({
    top: 0,
    behavior: 'smooth'
  })
}

export const scrollCourses = () => {
  window.scroll({
    top: 450,
    behavior: 'smooth'
  })
}

export const scrollAboutUs = () => {
  window.scroll({
    top: 1000,
    behavior: 'smooth'
  })
}

export const scrollContacts = () => {
  window.scroll({
    top: 2000,
    behavior: 'smooth'
  })
}